# Module removed

This module has been removed from the primary configuration. The file is
retained as an inert placeholder to avoid import-time errors in older
branches. If you want to remove it entirely, run `git rm` locally and
commit the change.

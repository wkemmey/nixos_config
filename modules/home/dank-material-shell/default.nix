{ config, lib, ... }:

# Module removed — intentionally left as an empty noop stub to avoid
# breaking older branches that might still import it. Remove files
# locally with `git rm` when you're ready to delete permanently.

{ config, lib, ... }:
{
  config = {};
}
